enum EnumGender { male, female, none }

enum EnumMediaType { image, video }

enum EnumCategory { tourist, resident, none }

enum EnumCallType { incoming, outgoing }

enum EnumCallStatus { pending, inprogress, completed, missed, failed }

enum EnumAlertStatus { white, blue, red }

enum EnumFeedTab { news, community }
